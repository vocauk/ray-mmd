// This section defines various constants used for visual effects and rendering parameters

// Light intensity range for a scene (from 1.0 to 10.0)
static const float mLightIntensityMin = 1.0;
static const float mLightIntensityMax = 10.0;

// Distance at which a light source is placed from the camera (1000 units)
static const float mLightDistance = 1000;

// Near and far planes for a light source (0.1 to 400.0 units)
static const float mLightPlaneNear = 0.1;
static const float mLightPlaneFar = 400.0;

// Temperature range for a color temperature adjustment (from 1000 to 40000 with a default value of 6600)
static const float mTemperature = 6600.0; // range 1000 ~ 40000 // Default value D66 used for pure white

// Minimum and maximum threshold values for bloom effect (from 0.0 to 10.0)
static const float mBloomThresholdMin = 0.0;
static const float mBloomThresholdMax = 10.0;

// Minimum and maximum intensity values for environmental light (from 1.0 to 6.2831852)
static const float mEnvLightIntensityMin = 1.0;
static const float mEnvLightIntensityMax = 6.2831852;

// Minimum and maximum intensity values for bloom effect (from 1.0 to 20.0)
static const float mBloomIntensityMin = 1.0;
static const float mBloomIntensityMax = 20.0;

// Bloom dirt mask intensity value (2.0)
static const float mBloomDirtMaskIntensity = 2.0;

// Maximum threshold value for ghosting effect in bloom (from 0.0 to 10.0)
static const float mBloomGhostThresholdMax = 10.0;

// Minimum and maximum exposure values (from 0.0 to 8.0)
static const float mExposureMin = 0.0;
static const float mExposureMax = 8.0;

// Inner and outer values for vignette effect (from 1.0 to 3.5)
static const float mVignetteInner = 1.0;
static const float mVignetteOuter = 3.5;

// Near and far planes for point light sources (1.0 to 400.0 units)
static const float mPointLightNear = 1.0;
static const float mPointLightFar  = 400.0;

// Minimum and maximum distance values for PSSM (Percentage-closer soft shadows) cascade rendering (5 to 1000 units)
static const float2 mPSSMCascadeDistance = float2(5, 1000); // x : min, y : max

// Split values for PSSM cascade rendering (from 0.04 to 0.7)
static const float4 mPSSMCascadeSplits = float4(0.04, 0.1, 0.16, 0.7);

// Minimum and maximum depth values for PSSM rendering (from 0 to 2000 units)
static const float mPSSMDepthZMin = 0;
static const float mPSSMDepthZMax = 2000.0;

// Maximum range value for screen-space reflections (from 0 to 200 units)
static const float mSSRRangeMax = 200.0;

// The fade start value for screen space reflections
static const float mSSRFadeStart = 0.8;

// The minimum and maximum intensity values for screen space subsurface scattering
static const float mSSSSIntensityMin = 0.25;
static const float mSSSSIntensityMax = 0.02;

// The minimum and maximum radii for ambient occlusion
static const float mAmbientOcclusionMinRadius = 0.1;
static const float mAmbientOcclusionMaxRadius = 100.f;

// The maximum radius, as a percentage of screen width, for ambient occlusion
static const float mAmbientOcclusionMaxRadiusPixelPercentage = 0.1f;

// The bilateral sharpness value for ambient occlusion
static const float mAmbientOcclusionBilateralSharpness = 8.f;

// The bias value for ambient occlusion
static const float mAmbientOcclusionBias = 0.2;

// The minimum and maximum intensity values for ambient occlusion
static const float mAmbientOcclusionIntensityMin = 0.0;
static const float mAmbientOcclusionIntensityMax = 10.0;

// The subpixel quality value for fast approximate anti-aliasing
static const float mFXAAQualitySubpix = 0.5;

// The edge threshold values for fast approximate anti-aliasing
static const float mFXAAQualityEdgeThreshold = 0.166;
static const float mFXAAQualityEdgeThresholdMin = 0.0333;

// The width and height of the sensor used for camera rendering
static const float mSensorWidth = 36.0f; // 35mm "full-frame" film format is 36mm x 24mm
static const float mSensorHeight = 24.0f;

// The minimum and maximum exposure values for eye adaptation
static const float mEyeAdapationMinExposure = 0.0;
static const float mEyeAdapationMaxExposure = 8.0;

// The minimum and maximum brightness values for eye adaptation
static const float mEyeAdaptationMinBrightness = 0.01f;
static const float mEyeAdaptationMaxBrightness = 100.f;